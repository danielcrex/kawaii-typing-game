/**
 * HUD (PROJECT.md §4 render/hud.ts).
 *
 * Hearts (gold-outlined, with regen sliver), the mono instrument readout
 * (WPM / accuracy / score), streak meter, and the ambient mascot stage.
 *
 * SCAFFOLD STATE: stub — implemented in §12 step 4.
 */
export {};
