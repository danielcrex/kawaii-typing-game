/**
 * Candy-capsule tile rendering (PROJECT.md §4 render/tiles.ts, §7.3).
 *
 * The signature element: a soft 3D candy-glass bubble (inner glow + top
 * specular) holding its mascot + word, with spring physics — squash on spawn,
 * gentle stretch while falling, gold+accent confetti pop on clear. The matched
 * prefix fills left-to-right with --tile-fill (liquid-in-glass), so progress is
 * visible mid-word.
 *
 * SCAFFOLD STATE: stub — implemented in §12 step 2 (lifecycle) + step 3 (fill).
 */
export {};
