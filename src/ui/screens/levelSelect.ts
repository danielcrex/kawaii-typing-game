/**
 * Level select (PROJECT.md §7.5) — locked/unlocked levels with gold star
 * ratings. SCAFFOLD STATE: stub; built in §12 step 8 (needs persistence).
 * For now the title's mascot grid stands in as the selector.
 */
export {};
