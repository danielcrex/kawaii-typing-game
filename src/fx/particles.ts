/**
 * Canvas particle FX (PROJECT.md §4 fx/particles.ts).
 *
 * The thin <canvas id="fx"> overlay handles what DOM struggles with: confetti
 * on tile clears and level-complete, sparkle bursts, gold+accent pops (§7.3).
 * Everything visible/interactive stays in DOM; this is pure decoration.
 *
 * SCAFFOLD STATE: stub — implemented in §12 step 7. Honors reduced-motion.
 */
export {};
